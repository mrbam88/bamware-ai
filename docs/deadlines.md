# Deadlines

Dated items Discord reminds Bilal about (#38). `scripts/discord-deadlines.sh`
posts at 7, 3 and 1 day(s) before, on the day, and once when overdue, with an
@mention when `DISCORD_USER_ID` is set in `~/.config/bamware/discord.env`.
Agents: add a row whenever a hard date appears (store deadlines, renewals,
reviews); delete it when done.

| Date | What | Link |
|---|---|---|
| 2026-09-30 | Google Play: register `io.bamware.brewdesk` + signing key in Play Console (~5 min; unregistered apps removed globally) | STATE.md → Blocked on Bilal |
